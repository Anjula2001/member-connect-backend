package com.memberconnect.backend.enums;

/**
 * The three documents printed for a new member (MR15-17). Each maps to its own
 * "printed at" column on Member.
 */
public enum MembershipDocumentType {
    MEMBERSHIP_CARD,
    SIGNATURE_CARD,
    PASSBOOK
}
